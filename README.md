Replace index.html in /opt/technitium/dns/www & 
Replace main.css in /opt/technitium/dns/www/css

This will enable more usable screen with larger monitors and make using the GUI from mobile much easier.
